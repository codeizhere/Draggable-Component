"use client";
import { DraggableList } from "../components";

const Home = () => {
  return (
    <main className="flex p-5 bg-gray-100 h-screen">
      <DraggableList />
    </main>
  );
};

export default Home;
